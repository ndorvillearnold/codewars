
function maps(x) {
    return x.map(num => num * 2)
}

//Given an array of integers, return a new array with each value doubled.

function maps(x) {

    return x.map(element => element * 2)

}


const maps = [1, 2, 3, 4]
console.log(maps.map(element => element * 2))
